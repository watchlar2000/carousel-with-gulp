# carousel-with-gulp
Practice setting-yup the gulp.

## Getting started

1) Install all the required packages.
```
npm i
```

2) Run the code in either development or build mode.

```
npm run dev || npm run build
```
